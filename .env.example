# Gemini (Phase 2). Leave empty to run browse/search without the assistant.
# Never commit real keys. Never put GEMINI_API_KEY in frontend code.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Razorpay Test Mode (Phase 5). Leave empty until checkout.
# Never commit real keys. Never put RAZORPAY_KEY_SECRET in frontend code.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
